Pour compiler le fichier javascript se trouvant dans source/class/vigigraph/Application.js:
    - Récupérer le framework qooxdoo à cette URL:
        http://downloads.sourceforge.net/project/qooxdoo/qooxdoo-legacy/0.7.3/qooxdoo-0.7.3-sdk.tar.gz
    - Détarer celui-ci:
        tar -zxf qooxdoo-0.7.3-sdk.tar.gz
    - Compiler via la commande suivante:
        make build


Pour utilisier le javascript compilé il faut le déplacer (ainsi que les icônes utilisées) à l'endroit voulu "vigigraph/public/js"
    - Le javascript compilé est disponible dans les répertoires suivant:
        build/script/vigiraph.js
    - Les icônes utilisées sont dans:
        build/resource

Pour utiliser le javascript non compilé (mode de debug):
    - Générer le fichier vigigraph.js "dynamique" (pointant vers chaque fichier .js utilisé)
        make
    - L'url à utiliser est alors http://<MonAncienneURL>/source
